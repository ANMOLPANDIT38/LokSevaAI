{
  "version": 2,
  "builds": [
    {
      "src": "react/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "react/build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/react/$1"
    }
  ]
}
